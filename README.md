@@
 ### POST /api/bounties/:id/refund
-  *No documentation yet*
+  Refunds a bounty that has been reserved but not yet paid out.  
+  The bounty id is supplied as a path parameter.  
+  The request body must be empty (no JSON payload).  
+  
+  #### Request
+  