import { Router } from 'express';

const router = Router();

// Bounty-specific routes will be defined here.
// See notificationPreferences.ts for push notification preferences.

export default router;
